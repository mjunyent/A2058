#pragma once

#include "../Tobago.h"

using namespace glm;
/*
class Model {
	public :
		VBO *vertexs;
		VBO *normals;
		VBO *texCoords;

		IBO *indexs;

		TBO *specular_texture;

		vec3 ambient_color;
		vec3 difuse_color;
		vec3 specular_color;
		float shininess;

		mat4 Model(VBO *vertexs, VBO *normals, IBO *indexs, vec3 ambient_color, vec3 difuse_color, vec3 specular_color, float shininess);

		void render();
};
*/